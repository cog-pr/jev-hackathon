{
  "name": "jev-hackathon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env server.mjs",
    "check-api": "node --env-file=.env index.mjs"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0",
    "express": "^5.1.0"
  }
}
