# Jev Hackathon 仕様書

## 1. プロダクト概要

### コンセプト

**Todoを管理するのではなく、「今、何から手をつけるべきか」を決めてくれる学生向けタスクアプリ。**

大学生活では、課題・授業・就活・資格勉強・イベント・私生活など、性質の違うタスクが同時に発生する。一般的なTodoアプリはタスクを一覧化できても、最終的に「どれからやるか」はユーザー自身が判断しなければならない。

本アプリでは、タスク情報とユーザーの現在状況をJevに渡し、各タスクの緊急性・重要性・今取り組む適切さなどを構造化して判定する。その結果をもとに「今やる順」を提示し、着手時の迷いを減らす。

### プロダクトの本質

本プロダクトの本質は、タスクを保存・整理することではない。

> **「今、何をやるか」という意思決定を、ユーザーの代わりに行うこと。**

タスクの一覧化はそのための手段にすぎない。最終的にユーザーに提供したい体験は、アプリを開いた瞬間に

``` text
じゃあ今はこれをやる
```

と提示され、迷わず着手できることである。

したがって本プロダクトの価値は、**ユーザーが入力・判断しなければならない量をどれだけ減らせたか**で評価する。機能が増えてもユーザーの入力や判断が増えるなら、それは本プロダクトにとって改善ではない。

------------------------------------------------------------------------

## 2. 解決したい課題

### 主な課題

-   Todoが増えるほど、何から始めるか迷う
-   締切だけでは優先順位を決められない
-   「重要だが今はできない」タスクがある
-   場所、残り時間、集中力などによって最適なタスクが変わる
-   優先順位を自分で毎回考えること自体が負担になる

### 想定ユーザー

大学生を中心とした、授業・課題・就活・資格・イベントなど複数種類のタスクを並行して抱える人。

------------------------------------------------------------------------

## 3. 一般的なTodoアプリとの違い

一般的なTodoアプリは「記録・整理」までを担当し、最後の意思決定はユーザーに残される。

``` text
一般的なTodoアプリ

  ユーザーが入力 → アプリが一覧表示 → ユーザーが判断 → 着手
                                        ^^^^^^^^^^^^^^
                                        ここが残る
```

本プロダクトは、この最後の判断をJevに任せる。

``` text
本プロダクト（最終形）

  自動で集まる → Jevが判断 → 「今これをやる」 → 着手
  ^^^^^^^^^^^^   ^^^^^^^^^^
  入力も減らす   判断も任せる
```

| 観点 | 一般的なTodoアプリ | 本プロダクト |
|---|---|---|
| 主な機能 | タスクの記録・整理・通知 | 「次に何をやるか」の意思決定 |
| 並び順 | 締切順・手動並べ替え・フラグ | 現在の状況を踏まえたJevの判定 |
| 状況の考慮 | しない（締切は状況で変わらない） | する（同じ一覧でも状況で順位が変わる） |
| ユーザーの役割 | 入力して、整理して、判断する | 提示されたものに着手する |
| 成功の指標 | 登録タスク数・完了率 | ユーザーが判断せずに着手できた回数 |

------------------------------------------------------------------------

## 4. Jevを使う理由

通常のTodoアプリでは、例えば以下のような固定ルールで優先順位を計算する必要がある。

`締切までの日数 × 重要度 × 所要時間 ...`

しかし実際の「今やるべきか」は、単純な数式だけでは決めにくい。

本アプリではJevを利用し、

**タスク情報 + ユーザーの現在状況 → 構造化された判断**

に変換する。

### Jevに任せる判断の例

-   このタスクは緊急か
-   このタスクは重要か
-   今この瞬間に取り組むのが適切か
-   後回しにしても問題が小さいか
-   タスクのカテゴリは何か

Jevが返した値をアプリ側で利用し、最終的な並び順を決定する。

### Jevが向いている処理と、向いていない処理

Jevは文章を生成するモデルではなく、**型のついた判断と確率を返す**モデルである。そのため役割分担を次のように整理する。

| 処理 | 担当 |
|---|---|
| 締切までの日数計算、並べ替え、表示、保存 | 通常のコード |
| 日付らしき文字列の候補抽出、正規化 | 通常のコード |
| 「今やるべきか」の判定、重要度・緊急度の評価 | Jev |
| 「この文章は自分に関係のある課題か」の判定 | Jev |
| 「候補のうちどれが締切か」の選択 | Jev |
| 長い文章の要約・言い換えの新規生成 | Jev以外（必要なら生成系AI） |
| メール・LMS・カレンダーからのデータ取得 | 外部API |

------------------------------------------------------------------------

## 5. Task と Schedule / Constraint の違い

本プロダクトには性質の異なる2種類の情報がある。両者を混同しないことが、プロダクトの範囲を保つ上で重要である。

**Task（タスク）** — ユーザーが取り組む候補であり、Jevが「今やるべきか」を判定する対象そのもの。タスク名・締切・所要時間・補足を持つ。

**Schedule / Constraint（予定・制約）** — 授業・バイト・通院など、その時間帯はタスクに着手できないことを表す制約情報。ユーザーが取り組む対象ではなく、Jevに渡す`currentContext`（`availableMinutes` / `nextEvent` / `currentEvent`）を生成するための入力にすぎない。

| 観点 | Task | Schedule / Constraint |
|---|---|---|
| 定義 | ユーザーが取り組む候補 | その時間を使えないことを表す制約 |
| Jevへの渡り方 | 判定対象として毎回渡す | `availableMinutes` / `nextEvent` / `currentEvent` に変換されて渡る |
| 画面上の扱い | やることリストに並び、優先順位がつく | 「次の予定まで80分」のような1行の導出結果としてのみ表示する |
| 完了という概念 | ある（着手し終わったらリストから消える） | ない（繰り返し続く） |
| 増えたときの意味 | 優先順位判定の材料が増える | コンテキストの精度が上がる |

### 予定機能の位置づけ

現在実装している繰り返し予定（曜日・開始時刻・終了時刻・ラベル）は、Google Calendarなど外部の予定情報をまだ取得できないMVPにおいて、`availableMinutes`と`nextEvent`を生成するための**仮のコンテキスト入力手段**である。

将来Google Calendar連携が実現すれば、この予定入力UIはカレンダーAPIからの自動取得に置き換わり、ユーザーが手で予定を登録する必要はなくなる。**予定機能そのものを拡張すること（月表示・週表示、タイムブロッキング、色分け、通知、参加者管理など）はプロダクトの目的ではない。** 予定はユーザーに見せるコンテンツではなく、Jevに渡すコンテキストを生成する装置として扱う。

------------------------------------------------------------------------

## 6. MVP

ハッカソンで最初に完成させる範囲は以下とする。

### 6.1 タスク追加

ユーザーが最低限以下を入力できる。

-   タスク名
-   詳細（任意）
-   締切
-   所要時間の目安
-   タスクの重要度に関係する補足（任意）

例：

``` text
タスク名：数学レポート
締切：明日 23:59
所要時間：120分
補足：成績の30%を占める
```

### 6.2 現在状況の入力

優先順位判定に利用する現在の状態を入力する。

-   現在時刻
-   今使える時間
-   集中力
-   現在地・作業環境
-   PCを利用できるか

MVPでは一部を省略してもよい。

### 6.3 Jevによる判定

各タスクについてJevへ判定を依頼する。

想定する質問：

``` text
urgent:
このタスクは緊急ですか？

important:
このタスクは重要ですか？

shouldDoNow:
現在のユーザーの状況を考慮すると、このタスクに今取り組むべきですか？

canPostpone:
このタスクを後回しにしても問題は小さいですか？
```

必要に応じて `Noul`、`Choice`、`Score` を使い分ける。

> 実装時の決定：上記を個別のNoulで取得して独自に重み付けする代わりに、
> **「今この状況でどれくらい優先して取り組むべきか」を4段階のScore1つで判定し、
> そのscore降順で並べる**方式を採用した。ランキングそのものをJevに委ねることで、
> アプリ側に独自の重み付け式を持たせないためである。

### 6.4 「今やる順」の表示

Jevの判定結果を利用し、タスクを優先順に表示する。

表示例：

``` text
🔥 今からやる

1. 数学レポート
   明日締切 / 約2時間
   今やるべき度：高

2. 就活ES
   3日後締切 / 約1時間
   今やるべき度：中

----------------

あとでOK

3. IT資格の勉強
4. 部屋の掃除
```

------------------------------------------------------------------------

## 7. 現在のMVPの課題

MVPを実際に触って判明した、最も重要な課題を記録する。

### 入力コストが価値を上回っている

現在のMVPでは、ユーザーが以下をすべて手入力する必要がある。

``` text
タスク側：タスク名 / 締切 / 所要時間 / 補足
状況側：現在時刻 / 使える時間 / 集中力 / 場所 / PCの有無
```

しかし、**ここまで情報を書き出した時点で、ユーザー自身にもある程度「何をやるべきか」が分かってしまう**。

意思決定の材料を人間が全部並べ終えてから「では何をやりますか」と尋ねる構造になっており、本来なくしたかったはずの「考える負担」がアプリを使う前に発生している。入力コストに対して、得られる価値が見合っていない。

### Todoアプリとしての高機能化は解決にならない

この課題は、入力フォームを使いやすくしたり、タグ・繰り返し・リマインダーなどのTodo機能を足したりしても解決しない。それらはいずれも「ユーザーがより多く入力・管理する」方向の改善であり、本プロダクトの本質と逆行する。

解決すべきなのは、

> **そもそもユーザーが入力しなくてよい状態をどう作るか**

である。

### 現在の状況入力の多くは自動化できる

現在ユーザーに尋ねている項目のうち、本質的に人間しか知り得ない情報は少ない。

| 入力項目 | 本来の取得方法 |
|---|---|
| 現在時刻 | 自動取得できる |
| PCの有無 | 利用デバイスから推定できる |
| 使える時間 | カレンダーの次の予定までの空き時間から算出できる |
| 場所 | 位置情報・時間帯・予定から推定できる |
| 集中力 | 時間帯や直近の活動から推定できる（確度は低い） |
| 体調・気分 | ユーザーしか知らない |

つまり、ユーザーに本当に尋ねるべきなのは「今30分だけ空いた」「今日は疲れている」といった、**自動取得では分からない差分情報だけ**である。

------------------------------------------------------------------------

## 8. 重要な設計思想

### 「重要度ランキング」だけにしない

本アプリが判断したいのは単純な重要度ではなく、

> **今、この状況の自分が何をやるべきか**

である。

例えば「明日締切の2時間かかるレポート」が最重要でも、現在が電車内で残り20分、PCなしなら、今できる別タスクを先に提示する。

### 状況によって順位が変化する

同じタスク一覧でもユーザーの状況によって結果が変わることを、本プロダクトの特徴とする。

例：

``` text
朝・自宅・PCあり
→ レポート作成

電車・30分・PCなし
→ ESの構成を考える

就寝前・15分
→ 明日の提出物を確認する
```

### 入力を増やす改善を、改善と呼ばない

機能追加を検討する際は、常に次の基準で判断する。

-   ユーザーの入力・判断が**減る**なら、改善である
-   ユーザーの入力・判断が**増える**なら、どれだけ高機能でも採用しない
-   同じ情報を毎回入力させているなら、それは自動化の対象である

------------------------------------------------------------------------

## 9. JevへのState設計案

``` text
現在日時
利用可能時間
集中力
場所
利用可能デバイス

タスク一覧
- タスク名
- 詳細
- 締切
- 所要時間
- 補足情報
```

例：

``` json
{
  "currentContext": {
    "availableMinutes": 80,
    "focus": "medium",
    "location": "大学",
    "hasPC": true,
    "currentEvent": null,
    "nextEvent": { "label": "バイト", "startsAt": "16:00" }
  },
  "task": {
    "title": "数学レポート",
    "deadline": "2026-09-21T23:59:00",
    "estimatedMinutes": 120,
    "note": "成績の30%を占める"
  }
}
```

`currentEvent` / `nextEvent`は、5章のSchedule / Constraintから自動算出される。ユーザーが「今使える時間」を毎回入力しなくても、この2つのフィールドが埋まる。

将来的に入力が自動化されても、このStateの構造自体は変わらない。変わるのは**誰がこのStateを埋めるか**（ユーザーか、外部サービスとコードか）だけである。意思決定エンジンとしてのJev部分を、入力手段から切り離して設計しておく。

------------------------------------------------------------------------

## 10. 画面構成案

### メイン画面

最重要画面。「今やるべきこと」を最上部に大きく表示する。

``` text
┌─────────────────────────┐
│ 今、何やる？             │
│                         │
│ 🔥 数学レポート          │
│ 明日まで・約2時間         │
│                         │
│ [これを始める]            │
└─────────────────────────┘

次にやる
2. 就活ES
3. 資格勉強

あとでOK
4. 部屋の掃除
```

### タスク追加画面

できるだけ入力負担を小さくする。最終的には、この画面を開く必要がない状態を目指す。

### 現在状況設定

「今使える時間」など、優先順位に影響する情報を変更できる。自動取得できるものは自動で埋め、ユーザーは差分だけを上書きする。

------------------------------------------------------------------------

## 11. 入力の自動化（将来像）

本プロダクトが目指す最終形は、

> **やるべきことが自動的に集まり、次に何をすべきかまで決めてくれるアプリ**

である。ユーザーがタスクを入力して管理するアプリではない。

### 11.1 タスクの自動収集（学生版インボックス）

タスクを手入力するだけでなく、大学生活で流れてくる情報を一か所に集約する。

想定情報源：

-   大学ポータル
-   LMS（Moodle 等）
-   メール
-   Discord
-   Google Classroom
-   シラバス
-   就活関連の連絡
-   イベント情報

受信した文章についてJevが、

-   自分に関係があるか
-   行動が必要か
-   締切があるか
-   緊急か
-   どのカテゴリか

を判断し、必要なものをタスク候補として提示する。

### 11.2 タスク情報の自動構造化

収集した文章から、以下を可能な限り自動で埋める。

| 項目 | 埋め方 |
|---|---|
| タスク名 | 件名・見出しなど、文章中の候補をコードが列挙し、Jevが最適なものを選択する |
| 締切 | 日付候補をコードが抽出・正規化し、Jevが「どれが締切か」を選択する |
| 内容 | 元の文章をそのまま保持する（生成しない） |
| 必要時間 | Jevが段階的なScoreで見積もる（例：15分以内 / 30分 / 1〜2時間 / 数時間以上） |
| カテゴリ | JevがChoiceで分類する |

文章を新しく生成するのではなく、**元の文章から選び取る**方式を基本とする。Jevは生成モデルではなく判定モデルであり、この方式のほうが誤りが混入しにくく、根拠も追跡できる。

### 11.3 現在状況の自動取得

| 項目 | 取得方法 |
|---|---|
| 現在時刻 | システムから自動取得 |
| 次の予定までの空き時間 | カレンダー連携から算出（MVPでは5章のSchedule / Constraint機能で先行実装済み） |
| 利用デバイス | 実行環境から判定 |
| 場所 | 位置情報、または予定から推定 |
| 集中力・体調 | ユーザーが必要なときだけ上書き |

ユーザーの入力は、最終的に

``` text
「今30分空いてる」
「今日は疲れている」
```

程度の、自動取得では分からない差分だけになることを目指す。

### 11.4 その他の拡張

-   完了履歴の蓄積
-   ユーザーの傾向を考慮した優先順位調整
-   「30分空いた」などのワンタップ状況変更
-   タスク開始・終了による再ランキング

------------------------------------------------------------------------

## 12. 今回のMVPの位置づけ

今回のMVPは、最終形のうち**「Jevによる意思決定部分」だけを取り出して検証するPoC**である。

``` text
最終形

  [ 収集 ]  →  [ 構造化 ]  →  [ 意思決定 ]  →  [ 提示 ]
   Moodle       Jev+コード       Jev           UI
   メール
   カレンダー

今回のMVP

  [ 手入力 ]  →  [ 意思決定 ]  →  [ 提示 ]
   仮のUI          Jev           UI
                  ^^^^^^^^^^^^^^^^^^^^^^
                  ここを検証する
```

### 検証したいこと

-   同じタスク一覧でも、現在の状況によって順位が変わるか
-   その順位変化が、人間の感覚として納得できるか
-   Jevの構造化された判定を、そのまま画面表示に使えるか

### 検証しないこと（今回のスコープ外）

-   タスクの自動収集
-   入力UIとしての完成度
-   タスク管理機能としての網羅性

### 現在の手入力UIの扱い

**現在のタスク追加・状況入力のUIは最終形ではなく、意思決定エンジンを検証するための仮の入力インターフェースである。**

したがって、この入力UIを「Todoアプリとして使いやすくする」方向の改善には投資しない。投資するのは、

-   Jevの意思決定そのものの質を上げる改善
-   将来の自動入力につながる改善
-   入力そのものをなくす改善

に限る。

------------------------------------------------------------------------

## 13. ハッカソンでの優先順位

### Must

1.  タスクを追加できる
2.  Jev APIにタスクと現在状況を送れる
3.  Jevの構造化された判定を取得できる
4.  判定結果から「今やる順」を表示できる

### Should

-   現在使える時間を変更すると順位が変わる
-   Jevの判定値をUIで確認できる
-   タスク完了機能

### Could

-   自然文を貼り付けてタスク化
-   学生版インボックス
-   カレンダー連携
-   外部サービス連携

------------------------------------------------------------------------

## 14. デモシナリオ

複数タスクを登録する。

``` text
数学レポート
締切：明日
所要時間：2時間

就活ES
締切：3日後
所要時間：1時間
志望度：高

資格勉強
試験：2週間後

部屋の掃除
締切：なし
```

最初は、

``` text
現在：自宅
使える時間：3時間
PC：あり
集中力：高
```

として判定する。

→ 「数学レポート」が上位になる。

次に状況だけを、

``` text
現在：電車
使える時間：30分
PC：なし
集中力：低
```

へ変更して再判定する。

→ 順位が変化する。

この変化によって、

**「締切順に並べるTodoではなく、現在の状況まで理解して次の行動を決める」**

という本アプリの価値とJevの役割を短時間で示す。

------------------------------------------------------------------------

## 15. プロダクトの一言説明

> **Todoを管理するのではなく、次にやることを決めてくれる学生向けタスクアプリ。**

将来像まで含めると、

> **やるべきことが自動的に集まり、次に何をすべきかまで決めてくれるアプリ。**

### 仮タイトル

未定。実装後にプロダクトの方向性に合わせて決定する。
